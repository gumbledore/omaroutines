#!/bin/bash
# systemctl stub: logs argv; `--user start omaroutines-herdr.service` brings
# the herdr stub's server "up" by removing $STUB_DIR/herdr-down.
printf 'systemctl %s\n' "$*" >>"$STUB_LOG"
[[ "$*" == *" start omaroutines-herdr.service"* ]] && rm -f "$STUB_DIR/herdr-down"
exit 0
